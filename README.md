## Overview
* Push more often
* Git init
* how to revert a commit
* Git Collaboration
* Git firedrill


```
git checkout -b firedrill
git add .
git commit -m "firedrill"
git push -u origin firedrill
```

